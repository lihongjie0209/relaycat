version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - gosec
    - misspell
    - nilerr
    - staticcheck
formatters:
  enable:
    - gofmt
    - goimports
